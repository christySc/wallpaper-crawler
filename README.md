# Wallpaper Crawler

一个简单的 Python 壁纸图片爬虫。

## 安装

```bash
pip install -r requirements.txt
```

## 使用

```bash
# 默认爬取
python main.py

# 指定 URL 和数量
python main.py -u "https://example.com/wallpapers" -n 10
```

## 配置

编辑 `config.py` 修改目标 URL、下载目录、请求间隔等参数。
